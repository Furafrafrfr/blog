import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    // type: "light",
    primary: {
      main: "#e65100",
    },
    secondary: {
      main: "#2196f3",
    },
  },
});

export default theme;
